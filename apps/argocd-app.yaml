apiversion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: BlueGreen-demo-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/aws-devops-project/argo-rollout-blue-green.git'
    targetRevision: HEAD
    path: apps
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true   